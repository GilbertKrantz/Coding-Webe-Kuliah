# noviyanti can be replaced with your Docker Hub username. Make sure to include the period (.) after the tag name (space)
1. docker build -t noviyanti/test_image .

# run the image. When executed, you should get an output saying "Hellow rolds!. The current time is --blah. blah."
2. docker run noviyanti/test_image

3. docker pull jupyter/pyspark-notebook

# Ensure the image exists
4. docker images

#Establish docker connection with port 7777
5. docker run -p 7777:8888 jupyter/pyspark-notebook:latest

6. docker run -p 8888:8888 jupyter/pyspark-notebook:latest

#ctrl + click one of URLs provided in the terminal to access the notebook

#Use the following final command when you run Docker. 
docker run -it --rm --shm-size=1g --ulimit memlock=-1 -p 8888:8888 jupyter/pyspark-notebook:latest
 